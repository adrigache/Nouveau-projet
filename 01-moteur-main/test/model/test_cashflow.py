from mca_model.model.cash_flow import entries as cf



def test_cashflow(model):
    """dummy - test if functions exist"""
    pass
    # cf.operating_cf(model)
    # cf.CFADS(model)
    # cf.CF_available_for_MRA(model)
    # cf.CF_available_for_MDRA(model)
    # cf.CF_available_for_junior_facility(model)
    # cf.CF_available_for_shareholders(model)
    # cf.cash_proceeds_account_EoP(model)
